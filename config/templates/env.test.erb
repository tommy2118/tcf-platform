# Test Environment Configuration  
# TCF Platform - Test Settings

# Environment
ENVIRONMENT=test
POSTGRES_PASSWORD=test_password

# Test Database Configuration
TEST_DATABASE_CLEANER=true
POSTGRES_USER=tcf
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_SSL=disable

# Redis Configuration
REDIS_HOST=localhost
REDIS_PORT=6379

# Test API Keys (Safe dummy values)
OPENAI_API_KEY=test-key-openai
ANTHROPIC_API_KEY=test-key-anthropic

# Application Secrets (Test-safe values)
JWT_SECRET=test-jwt-secret

# Service Configuration
DEFAULT_DAILY_TOKEN_BUDGET=1000
MAX_CONTEXT_SIZE=10000

# Test Settings
DEBUG_MODE=false
LOG_LEVEL=error
ENABLE_PROFILING=false
PARALLEL_TESTS=true

# Test Database Isolation
DATABASE_CLEANER_STRATEGY=transaction
TRANSACTIONAL_FIXTURES=true